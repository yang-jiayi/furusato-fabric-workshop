{
 "item の作成・配置、Notebook の完了、トリガーによる自動起動、Data Agent の応答、公開後の動作は別々に検証します。定義の作成成功やオフライン検証だけでは Core 完了になりません。未実施の工程は「未実施」、権限やサービス制約で進めない工程は「ブロック」として、理由を残してください。各工程は、実際に選んだ配置先と設定に対して確認してください。": "Verify item creation and placement, Notebook completion, automatic trigger activation, Data Agent responses, and post-publication behavior separately. Definition creation or offline validation alone does not complete Core. Mark unrun steps as unrun and steps prevented by permission or service constraints as blocked, with reasons. Check every step against the deployment target and configuration you actually selected.",
 "配置できたことと、動作を確認できたことを分ける": "Distinguish successful placement from verified operation",
 "ファイル検証や計画ハッシュの不一致で停止した場合は、原因を確認して preview を取り直します。計画が変わった場合の旧 checkpoint はファシリテーターが復旧記録として退避し、新計画の適用でも全バイトの SHA-256 照合を省略しません。既存資産や履歴を削除しません。": "If file validation or a plan-hash mismatch stops execution, investigate the cause and obtain a new preview. When the plan changes, the facilitator preserves the previous checkpoint as a recovery record. Do not omit full-byte SHA-256 verification when applying the new plan, or delete existing assets or history.",
 "5.2.1 既存 Workspace の指定 Folder に配置する場合": "5.2.1 Deploying into a designated Folder in an existing Workspace",
 "開始前に対象の Workspace 名・ID、Folder のパス・ID、PID を記録します。Fabric の Folder は item の整理単位であり、Lakehouse 内の `Files/increment` とは別物です。Folder は Workspace の権限を継承するため、専用 Workspace と同じ権限分離にはなりません。": "Before starting, record the target Workspace name and ID, Folder path and ID, and PID. A Fabric Folder organizes items; it is different from `Files/increment` inside a Lakehouse. Folders inherit Workspace permissions, so they do not provide the same permission isolation as dedicated Workspaces.",
 "実行先の Workspace / Folder は、配布物の参照例から推測せず、自分に割り当てられた場所を確認します。EXPECTED_WORKSPACE_NAME には、現在選んだ Workspace の実際の表示名を設定します。URL の `subfolderId` が数値の場合は REST の `folderId`（GUID）として使わず、UI の階層と API の Folder 一覧を突き合わせて対象を確定します。一意に確定できるまで書き込まず、別 Folder の既存 item で代用しません。": "Confirm your assigned Workspace / Folder rather than inferring the target from distribution examples. Set EXPECTED_WORKSPACE_NAME to the actual display name of the Workspace you have selected. If the URL has a numeric `subfolderId`, do not use it as the REST `folderId` (GUID); reconcile the UI hierarchy with the API Folder listing to identify the target. Do not write until the target is uniquely resolved or substitute an existing item in another Folder.",
 "実行先の Workspace と Folder を確定する": "Identify the target Workspace and Folder",
 "対象 Folder を開いてから［新規］や Notebook のインポートを行います。ホームや作成ハブから作ると Workspace 直下に置かれるため、作成後にも配置先を確認します。": "Open the target Folder before choosing ［New］ or importing a Notebook. Creation from the home page or Create hub places items at the Workspace root, so verify the location after creation as well.",
 "既存 item を Folder 外も含めて確認します。Notebook 02 と単独の Notebook 03 は、参照する item 名を Workspace 全体から完全一致で解決します。Folder が違っても同名なら安全とは限りません。他の実習と衝突する場合は停止し、既存 item を削除・改名して回避しません。": "Inspect existing items, including those outside the Folder. Notebook 02 and standalone Notebook 03 resolve referenced item names by exact match across the entire Workspace. Identical names are not necessarily safe merely because the Folders differ. Stop if there is a conflict with another exercise; do not work around it by deleting or renaming existing items.",
 "Notebook 03 の TARGET_FOLDER_NAME は作成先だけの指定です。Notebook 04 は自分自身の Folder を作成先にするため、先に対象 Folder へ配置します。Workspace 直下から実行してはいけません。EXPECTED_WORKSPACE_NAME を持つ Notebook では対象 Workspace 名も設定します。": "TARGET_FOLDER_NAME in Notebook 03 specifies only the creation destination. Notebook 04 uses its own Folder as the destination, so place it in the target Folder first. Do not run it from the Workspace root. For Notebooks with EXPECTED_WORKSPACE_NAME, also set the target Workspace name.",
 "作成後の item ID、親 item ID、最終 Folder を実環境で確認して記録します。自動生成される KQL Database・SQL analytics endpoint・GraphModel も親との対応を確認します。例の ID や表示名だけから ID を推測して接続先を埋めません。": "After creation, verify and record actual item IDs, parent item IDs and final Folders in the environment. Check the parent associations of automatically generated KQL Databases, SQL analytics endpoints and GraphModels too. Do not infer connection IDs from example IDs or display names alone.",
 "指定 Folder 外の既存 item は変更しません。自動生成された item が想定外の場所にある場合は、今回作成した ID と親子関係を確認してから、対応する移動手順で配置を整えます。移動できないものは例外として記録し、すべてが指定 Folder 内にあると報告しません。API が長時間操作を返した場合も、完了状態と実体の再取得を確認するまでは成功扱いにしません。": "Do not modify existing items outside the designated Folder. If an automatically generated item is in an unexpected location, verify its newly created ID and parent-child relationship before using the supported move procedure to correct its placement. Record items that cannot be moved as exceptions; do not report that everything is in the designated Folder. When an API returns a long-running operation, do not mark it successful until both completion status and re-reading the actual resource are verified.",
 "作成先と参照先を ID で確認する": "Verify creation and reference targets by ID",
 "Notebook 01 が完了していても、`GraphNotRefreshable` が出る、または Graph の source・node・edge が 0 のままなら、Graph の機能は未確認です。Ontology の定義作成やメタデータ登録の件数で代用しません。参照先の実 ID、Property source / Data source、Lakehouse 側の `sourceSchema` を確認します。修正後は Graph のコンパイルと更新ジョブが完了してから、本章の件数を照合します。更新を連打したり、item の存在だけで合格にしません。": "Even if Notebook 01 has completed, a `GraphNotRefreshable` error or Graph source, node, and edge counts remaining at 0 means Graph functionality is unverified. Do not substitute Ontology definition creation or metadata-registration counts. Check the actual referenced IDs, Property source / Data source, and the Lakehouse `sourceSchema`. After correction, wait for Graph compilation and refresh jobs to finish before reconciling this chapter's counts. Do not repeatedly trigger refresh or pass the gate merely because an item exists.",
 "Ontology の定義と Graph の機能を分けて確認する": "Verify Ontology definition and Graph functionality separately",
 "Mapping table は、対象 Lakehouse の `dbo` スキーマにある `ot_` テーブルを選びます。名前とキーだけでなく schema も参照先の一部です。": "For the Mapping table, select the `ot_` table in the target Lakehouse's `dbo` schema. The schema is part of the reference, along with names and keys.",
 "本ワークショップの Lakehouse を参照する静的バインディングと Relationship の contextualization では、`sourceSchema` を `dbo` として保存します。空文字の場合は、ファシリテーターと参照元を確認し、ID・メタデータ・無関係の定義を保持して修正します。Eventhouse の time-series バインディングまで一律に `dbo` へ変更してはいけません。Notebook 02 は semanticEnrichment だけを更新するため、schema やバインディングの修復には使いません。同じ完全定義を使う Notebook 04 でも、この schema の指定が必要です。": "For static bindings and Relationship contextualization referencing this workshop's Lakehouse, save `sourceSchema` as `dbo`. If it is empty, verify the source with the facilitator and correct it while preserving IDs, metadata and unrelated definitions. Do not indiscriminately change Eventhouse time-series bindings to `dbo`. Notebook 02 only updates semanticEnrichment; do not use it to repair schemas or bindings. Notebook 04 uses the same complete definition and requires the same schema setting.",
 "Lakehouse の sourceSchema を空にしない": "Do not leave the Lakehouse sourceSchema empty",
 "手動 run へ `Type`・`Subject`・`Source` を入力して成功しても、OneLake → Activator → Pipeline の自動起動を確認したことにはなりません。手動診断は別記録にし、受信イベントと自動 run の対応を確認できない場合は「トリガー未検証」のままにします。失敗 run でも一部の行が書かれる可能性があるため、再送の前にトリガーを Off にして `SourceFile` 別件数を確認してください。": "A successful manual run supplied with `Type`, `Subject`, and `Source` does not verify automatic initiation through OneLake → Activator → Pipeline. Record manual diagnostics separately and leave the trigger \"unverified\" if you cannot correlate a received event with an automatic run. Even a failed run may have written some rows, so turn the trigger Off and check counts by `SourceFile` before redelivery.",
 "手動診断はトリガーの代替証拠にしない": "Do not substitute manual diagnostics for trigger evidence",
 "Notebook 04 などで定義から作成した Reflex は、UI で既存ルールを開いて保存し、［Start］で起動します。定義の有効フラグだけで稼働したと判定しません。Run Pipeline のアクション引数に `IncrementFileName` が必要な場合は、既定値 `donation_events_001.csv` を設定します。`Type`・`Subject`・`Source` は OneLake イベントからの動的な対応付けを保持します。ファイル選択は非空の Subject を使い、既定値だけの手動 run で代用しません。保存・引数設定・Start をまとめて確認する手順であり、Start 単独の効果と断定しません。": "For a Reflex created from a definition, such as by Notebook 04, open the existing rule in the UI, save it, and select ［Start］. Do not infer operation from an enabled flag in the definition alone. If the Run Pipeline action requires `IncrementFileName`, set its default value `donation_events_001.csv`. Keep `Type`, `Subject`, and `Source` dynamically mapped from OneLake events. File selection uses a nonempty Subject; do not substitute a manual run using only the default. This procedure checks saving, argument configuration, and Start together; it does not establish the effect of Start in isolation.",
 "保存・Start とアクション引数を確認する": "Verify saving, Start, and action arguments",
 "以下はトリガーが未作成の場合の手順です。Notebook 04 などで作成済みなら、新規ルールを追加せず、既存ルールの保存・Start と引数を確認します。": "The following procedure applies when the trigger has not yet been created. If it already exists, for example from Notebook 04, do not add a new rule; verify saving, Start, and arguments on the existing rule.",
 "`invokeType` が `Manual` と表示されても、その項目だけで手動実行と決めません。OneLake の受信イベント、Activator の activation / アクション実行記録、Pipeline のジョブ ID、Subject・ファイル名・行数を対応付けます。イベントの受信だけ、定義の有効フラグだけ、実行履歴の集計グラフや実行種別の表示だけでは自動起動の証明になりません。ここで区別するのは実際の起動経路です。": "Even if `invokeType` displays `Manual`, do not classify the run as manually initiated from that field alone. Correlate the received OneLake event, Activator activation / action-execution record, Pipeline job ID, Subject, file name, and row count. An event receipt, an enabled definition flag, an aggregate run-history chart, or an invocation-type label alone does not prove automatic initiation. The distinction is the actual initiation path.",
 "invokeType ではなく実際の起動経路を確認する": "Verify the actual initiation path, not invokeType",
 "Notebook 03 / 04 の完全定義には、time-series Property と 98 件のsemanticEnrichment が既に含まれます。その経路では Notebook 02 の差分が 0 件でも異常ではありません。手動構築では第 14 章が先、完全定義の経路では既存定義の確認が先です。「今回追加した件数」と「現在の定義にある総件数」を区別して記録してください。": "The complete definition in Notebook 03 / 04 already includes the time-series Property and 98 semanticEnrichment objects. On that route, 0 differences in Notebook 02 is not an error. For manual construction, complete Chapter 14 first; on the complete-definition route, inspect the existing definition first. Record \"objects added in this run\" separately from \"total objects in the current definition\".",
 "代替構築後のメタデータは既存分も数える": "Include existing metadata after alternative provisioning",
 "新規 Agent の既定は Standard runtime です。リボンの［Runtime］で Preview を明示的に選びます。": "New Agents default to the Standard runtime. Explicitly select Preview in the ribbon's ［Runtime］ control.",
 "Preview を選べない、必要なソースを追加できない、または API が Data Agent の定義を受け付けない場合は、未完了の工程とエラーを記録します。ランタイムの選択は、ソース対応や API 対応を保証しません。API 構築の代わりにこの節の UI 手順で構成できた場合も、代替経路として区別し、同じ 3 ソース・選択要素・指示・Preview runtime と第 17 章の評価を確認します。Standard の結果や定義の作成成功だけで、Core の応答確認を済ませたことにしません。": "If Preview cannot be selected, a required source cannot be added, or the API rejects the Data Agent definition, record the incomplete step and error. Runtime selection does not guarantee source or API support. If the UI procedure in this section works instead of API provisioning, identify it as an alternative route and verify the same 3 sources, selected elements, instructions, Preview runtime, and Chapter 17 evaluation. Standard results or successful definition creation alone do not complete Core response verification.",
 "Preview・定義作成・応答確認は別のゲート": "Preview, definition creation, and response verification are separate gates",
 "ソース画面を開くと、内部の element ID や未選択の枝が変わる場合があります。比較では、参照先の実 ID、選択したテーブル・ビュー・Entity Type、指示、説明の一致を確認します。UI 内部の識別子を参照先 ID と混同せず、選択違い・参照先違い・説明の変更を無視して一致扱いにしません。": "Opening a source screen can change internal element IDs or unselected branches. Compare the actual referenced IDs, selected tables, views and Entity Types, instructions, and descriptions. Do not confuse internal UI identifiers with referenced-source IDs, or treat definitions as equal by ignoring different selections, referenced targets, or descriptions.",
 "画面の展開情報と参照先・選択内容を区別する": "Distinguish UI expansion details from referenced targets and selections",
 "保存後の定義で `codeInterpreterEnabled=false` が省略される場合でも、キーの欠落だけで有効・無効や定義の一致を判定しません。画面の Tools と有効な設定を確認し、Core では Code Interpreter が無効であることを記録します。差分を説明できない場合は評価を保留し、無効を確認せずに省略を受け入れません。": "Even if the saved definition omits `codeInterpreterEnabled=false`, do not infer enabled or disabled state, or definition equality, from the missing key alone. Inspect Tools in the UI and the effective settings, and record that Code Interpreter is disabled for Core. If the difference cannot be explained, hold evaluation; do not accept the omission without confirming the disabled state.",
 "省略された設定値は実際の設定で確認する": "Verify omitted values against the effective settings",
 "この章の表は期待値であり、今回の実行結果ではありません。記録票には対象 Workspace / Folder と Data Agent の実 ID、実施日時、Draft / Published、runtime を残します。Agent を作成できないなどの理由で質問を送っていない場合は、判定欄を埋めずに「未実施 / ブロック」と理由を実施サマリーへ記録してください。": "The tables in this chapter contain expected values, not results from the current run. Record the target Workspace / Folder, actual Data Agent ID, execution date and time, Draft / Published state, and runtime. If no question was submitted, for example because the Agent could not be created, leave the verdict blank and record \"not run / blocked\" and the reason in the execution summary.",
 "期待値と今回の実行証跡を混同しない": "Do not confuse expected values with current execution evidence",
 "数値が一致していても、各問の評価基準で必要とする期間・スコープ・指標名・根拠の提示が足りなければ合格条件を満たしません。サービスの汎用的なコンテンツブロックは、業務上の意味やデータの限界を説明できた証拠ではありません。ブロック文やエラーは秘密情報を除いて記録し、期待する拒否の説明がないものを自動的に PASS にしません。指示の修正は一般化し、テスト固有の質問文・ID・期待値を与えてはいけません。": "Correct numbers do not satisfy the pass criteria if the period, scope, metric labels, or supporting evidence required by that question's rubric are missing. A generic service content block does not prove that business meaning or data limitations were explained. Record block messages or errors with secrets removed, and do not automatically mark a response PASS when the expected refusal explanation is absent. Generalize instruction corrections; do not supply test-specific questions, IDs, or expected values.",
 "数値一致や汎用ブロックだけで PASS にしない": "Do not pass on numbers or generic content blocks alone",
 "先に Notebook 01 の静的テーブルと、第 11 章の Eventhouse / KQL Database・管理コマンドを準備します。この Notebook は参照元のデータや KQL オブジェクトを作りません。": "First prepare Notebook 01's static tables and the Eventhouse / KQL Database and management commands in Chapter 11. This Notebook does not create source data or KQL objects.",
 "対象 Folder に Notebook 03 を取り込み、PARTICIPANT_ID・EXPECTED_WORKSPACE_NAME と作成先を確認します。TARGET_FOLDER_NAME が空なら Notebook 自身と同じ場所が選ばれます。": "Import Notebook 03 into the target Folder and verify PARTICIPANT_ID, EXPECTED_WORKSPACE_NAME, and the creation destination. If TARGET_FOLDER_NAME is empty, the Notebook's own location is selected.",
 "CREATE_ONTOLOGY=False で［Run all］し、参照元・作成先・全定義の計画を確認します。別 Folder の同名 item も解決に影響するため、第 5.2.1 節の名前確認を省略しません。": "Select ［Run all］ with CREATE_ONTOLOGY=False and inspect the sources, destination, and complete-definition plan. Items with the same name in other Folders also affect resolution, so do not skip the name checks in section 5.2.1.",
 "新規作成を行う場合は CREATE_ONTOLOGY=True、CREATE_CONFIRMATION=\"yes\"、EXCLUSIVE_CREATE_WINDOW_CONFIRMED=True にして対話実行します。作成後に全定義の検証を確認し、Core の件数・増分・time-series・メタデータのゲートを通します。": "For new creation, set CREATE_ONTOLOGY=True, CREATE_CONFIRMATION=\"yes\", and EXCLUSIVE_CREATE_WINDOW_CONFIRMED=True, then run interactively. After creation, verify the complete definition and pass the Core count, increment, time-series, and metadata gates.",
 "Notebook 04 を対象 Folder に取り込み、その場所から開きます。Notebook の実 ID と実行コンテキストが取得できない場合は停止し、安全ガードを外しません。": "Import Notebook 04 into the target Folder and open it there. If its actual ID and execution context cannot be obtained, stop; do not remove the safety guards.",
 "Fabric の UI が `exceeds the 500k limit` のようなセルサイズエラーで［Run all］を拒否した場合、Notebook 04 のジョブはまだ始まっていません。配布 Notebook を取り込み直し、対象 Folder・PID・実行計画を確認して preview からやり直します。完全なインライン payload を複数セルに分けた構成を保持してください。内容の削除、外部 URL からの取得への置き換え、対話実行ゲートの解除で回避しません。セル番号だけに頼らず、パラメーターセルと見出しを確認して操作してください。起動後に error 出力がある実行は未開始と区別して記録し、ジョブの最終状態も確認します。": "If Fabric UI rejects Run all with a cell-size error such as `exceeds the 500k limit`, the Notebook 04 job has not started. Reimport the supplied Notebook, confirm the target Folder, PID and plan, and restart from preview. Retain the complete inline payload distributed across multiple cells. Do not delete payload content, replace it with external URL retrieval, or disable interactive-execution gates to work around the limit. Identify the parameter cell and headings instead of relying only on cell numbers. Distinguish executions that started and produced error output from jobs that never started, and check the final job state.",
 "セルサイズ拒否と実行後のエラーを区別する": "Distinguish cell-size rejection from errors after execution starts",
 "PARTICIPANT_ID と EXPECTED_WORKSPACE_NAME を設定し、APPLY_CHANGES=False のままEXECUTE_NOTEBOOK_01・CREATE_PIPELINE・REFRESH_GRAPH・CREATE_DATA_AGENT・CREATE_REFLEX をすべて True にします。": "Set PARTICIPANT_ID and EXPECTED_WORKSPACE_NAME, leave APPLY_CHANGES=False, and set EXECUTE_NOTEBOOK_01, CREATE_PIPELINE, REFRESH_GRAPH, CREATE_DATA_AGENT, and CREATE_REFLEX all to True.",
 "この最終構成で preview を実行し、対象 Folder・既存 item の扱い・PLAN_SHA256 を確認します。フラグを後から変えた場合はハッシュも変わるため、preview をやり直します。": "Run preview with this final configuration and verify the target Folder, handling of existing items, and PLAN_SHA256. If a flag changes afterwards, the hash also changes, so repeat preview.",
 "CONFIRMED_PLAN_SHA256 に直前の値を貼り、APPLY_CHANGES=True とEXCLUSIVE_CREATE_WINDOW_CONFIRMED=True にして対話実行します。部分的な live apply は行いません。": "Paste the immediately preceding value into CONFIRMED_PLAN_SHA256, set APPLY_CHANGES=True and EXCLUSIVE_CREATE_WINDOW_CONFIRMED=True, and run interactively. Do not perform a partial live apply.",
 "今回の実行のジョブが最終成功状態であり、全セルの出力に error がないことを確認します。セルの finished / available だけでは成功を証明できません。InProgress の実行や古い preview 出力を apply の成功証拠にしません。実 item ID・配置先・子 Notebook の結果・Graph 更新も確認します。途中で止まった場合は作成済み資産と checkpoint を調べ、全体成功や自動ロールバックと見なしません。": "Verify that the current run's job is in a terminal success state and that no cell output contains an error. A cell's finished / available metadata alone does not prove success. Do not use an InProgress execution or old preview outputs as evidence of a successful apply. Also verify actual item IDs, placement, child Notebook results, and Graph refresh. If execution stops partway through, inspect created assets and the checkpoint; do not assume overall success or automatic rollback.",
 "Notebook 04 は、生成する Notebook 01 のコピーへ指定した PARTICIPANT_ID と Default Lakehouse を設定します。配布元の Notebook 01 自体は書き換えません。生成されたコピーのパラメーターセルと Lakehouse 設定を開き、どちらも今回の対象と一致することを確認します。Lakehouse の名前・ID が合っているだけでは、子 Notebook の PID が正しい証拠にはなりません。不一致なら先へ進まず、生成定義と実行履歴を確認してください。": "Notebook 04 sets the specified PARTICIPANT_ID and Default Lakehouse in the generated copy of Notebook 01. It does not modify the distributed Notebook 01 itself. Open the generated copy's parameter cell and Lakehouse settings, and verify that both match the current target. A matching Lakehouse name and ID alone does not prove that the child Notebook's PID is correct. If either differs, stop and inspect the generated definition and execution history.",
 "子 Notebook の PID と Lakehouse を両方確認する": "Verify both the child Notebook's PID and its Lakehouse",
 "Notebook 04 の［Run all］後に `NameError` が出た場合、パラメーターセルが画面に見えていても、そのセッションで実行されたとは限りません。前のジョブと checkpoint を確認し、APPLY_CHANGES=False に戻してパラメーターセルを明示的に実行してから preview をやり直します。これは起動後のエラーであり、実行前のセルサイズ拒否とは区別します。再発する場合は先へ進まず記録し、古い計画ハッシュや成功結果で補完したり、ゲートを外したりしません。": "If `NameError` occurs after ［Run all］ in Notebook 04, a parameter cell visible in the UI has not necessarily executed in that session. Check the previous job and checkpoint, restore APPLY_CHANGES=False, explicitly execute the parameter cell, and repeat preview. This is an error after startup, distinct from a pre-execution cell-size refusal. If it recurs, stop and record it; do not substitute an old plan hash or successful result, or remove the gates.",
 "パラメーターセルの表示と実行済みを混同しない": "Do not confuse a visible parameter cell with an executed one",
 "Notebook 04 は増分 3 CSV を `Files/_provisioning/furusato/<PID>/increment` に待機させます。監視先の `Files/increment` は作成するだけで、増分 CSV は置きません。INCREMENTS_STAGED は待機ファイルの配置を示す出力であり、Eventhouse への取り込みやFileCreated による起動、Notebook 04 全体の成功を証明するものではありません。": "Notebook 04 stages the 3 increment CSVs in `Files/_provisioning/furusato/<PID>/increment`. It creates the watched `Files/increment` folder without placing increment CSVs there. INCREMENTS_STAGED indicates that staging files have been placed; it proves neither ingestion into Eventhouse, a FileCreated-triggered start, nor overall Notebook 04 success.",
 "増分は監視対象外で待機させる": "Stage increments outside the watched location",
 "初回のトリガー検証では、配布原本を保持し、対象 Reflex が Off で、対象 KQL Database の DonationEvents にまだ行がないこと、対象 Pipeline の最新の実行履歴で run が 0 件であること、`Files/increment` が空であることを確認します。その後に既存ルールを UI で保存し、［Start］で起動して Running を確認してから、第 12.4〜13 章どおり配布された 3 CSV を 1 本ずつ新規アップロードします。各ファイルの自動 run と取り込み件数を確認してから次へ進み、同じファイルを重ねて置きません。": "For the first trigger verification, retain the original distribution files and confirm that the target Reflex is Off, DonationEvents in the target KQL Database has no rows yet, the target Pipeline's freshly retrieved run history contains 0 runs, and `Files/increment` is empty. Then save the existing rule in the UI, select ［Start］, verify Running, and upload the 3 distributed CSVs as new files, 1 at a time, following sections 12.4–13. Verify each file's automatic run and ingested count before proceeding to the next file; do not place the same file again.",
 "`Files/increment` に既存ファイルがある場合、Notebook 04 はそれを自動削除しません。Reflex の停止を確認し、現在の DonationEvents が 0 行、対象 Pipeline の run が 0 件であることを確認した初回検証だけ、配布原本と SHA-256 が一致する未取り込みファイルを記録とともに監視対象外へ退避します。ファイルや履歴の削除・上書きはしません。既に取り込み済みの行がある場合はこの初期化を行わず、履歴と SourceFile 別件数から復旧を判断します。既存ファイルへの上書きを新規 FileCreated と同一視しません。": "Notebook 04 does not automatically delete existing files in `Files/increment`. Only for an initial check with Reflex stopped, DonationEvents at 0 rows and the target Pipeline at 0 runs, move un-ingested files matching the distributed originals' SHA-256 outside the watched path and record the move. Do not delete or overwrite files or history. If any rows have already been ingested, do not perform this initialization; determine recovery from run history and SourceFile counts. Overwriting an existing file is not equivalent to a new FileCreated event.",
 "OneLake の全ファイル検証は、`fs.cp` でドライバーへバイトを保持してコピーし、ローカルの全バイト列で SHA-256 を照合します。`fs.head` の部分表示で代用せず、検証エラーだけを理由に正しい CSV を削除・再アップロードしないでください。一覧 API が `UnsupportedOperationForSchemasEnabledLakehouse` を返す場合は、defaultSchema を取得し、OneLake の `Tables/<schema>` と `_delta_log` を確認します。この API エラーだけでテーブル消失や Notebook 01 の失敗と決めません。schemas を Off にしたり Lakehouse を作り直して回避しません。既存データを保持し、ファイルとテーブルを完全に照合します。ディレクトリの存在だけで合格にせず、第 6.5 節の件数・金額も照合します。": "For complete OneLake file verification, use `fs.cp` to copy bytes unchanged to the driver, then check SHA-256 over all local bytes. Do not substitute the partial display from `fs.head`, or delete and re-upload a correct CSV solely because validation failed. If the listing API returns `UnsupportedOperationForSchemasEnabledLakehouse`, obtain defaultSchema and inspect OneLake `Tables/<schema>` and `_delta_log`. This API error alone does not establish table loss or Notebook 01 failure. Do not turn schemas Off or recreate the Lakehouse as a workaround. Retain existing data and fully reconcile files and tables. Directory existence alone is insufficient; also compare the counts and amounts in section 6.5.",
 "OneLake の全バイト検証と schema 対応": "Full-byte OneLake verification and schema support",
 "上の機能アイテム数は Notebook 04 の契約値であり、Optional 全体の配置数や Core 合格数ではありません。参照モード False は計画上 8 点、True は別 AIPath を 1 点加えた 9 点です。自動生成 GraphModel はこの計画数に含めず、False は 1 点、True は 2 点です。対象外の既存アイテムを含む Workspace 全体の inventory 件数とは区別します。Notebook 02 / 03 / 05、Power BI、UDF、Variable Library は実際に選んだ工程ごとに確認します。Data Agent の定義が作成できても、Preview の応答・10 問・公開後の確認は第 16〜18 章で別途実施します。": "The functional item count above is a Notebook 04 contract value, not a count of all deployed Optional items or Core passes. Reference mode False plans 8 items; True adds 1 separate AIPath for 9 items. Generated GraphModels are excluded from these plan counts: False has 1 and True has 2. Distinguish these counts from the entire Workspace inventory, which includes existing out-of-scope items. Verify Notebook 02 / 03 / 05, Power BI, UDF, and Variable Library for each step actually selected. Even if the Data Agent definition is created, separately verify Preview responses, the 10 questions, and post-publication behaviour in Chapters 16–18.",
 "一括構築後も機能ゲートは省略しない": "Do not skip functional gates after bulk provisioning",
 "D.3.2 Notebook 05 を実行して公開状態を確認する": "D.3.2 Run Notebook 05 and verify publication state",
 "`SHOW SCHEMAS` は `dbo` ではなく `<Workspace>.<Lakehouse>.dbo` のような完全修飾名を返す場合があります。名前の見え方だけで schema 非対応と決めません。対象 Workspace と Default Lakehouse の実 ID を確認したうえで、末尾の schema 名とバッククォート・大小文字を正規化して照合します。別の Lakehouse に切り替えたり、非 schema Lakehouse を拒否するガードを外したりして通しません。": "`SHOW SCHEMAS` may return fully qualified names such as `<Workspace>.<Lakehouse>.dbo` rather than `dbo`. Do not infer a lack of schema support from name formatting alone. Verify the target Workspace and actual Default Lakehouse ID, then normalize the terminal schema name, backticks and case before comparison. Do not switch to another Lakehouse or bypass the guard that rejects non-schema Lakehouses.",
 "SHOW SCHEMAS の完全修飾名を誤判定しない": "Do not misclassify fully qualified SHOW SCHEMAS names",
 "Notebook 01 または 04 の静的テーブルの準備を完了し、対象 Lakehouse を Notebook 05 の Default（ピン）に設定します。配置先 Folder と Default Lakehouse は別の設定です。": "Complete static-table preparation with Notebook 01 or 04, and set the target Lakehouse as Default (pinned) in Notebook 05. The placement Folder and Default Lakehouse are separate settings.",
 "対象 Lakehouse の `Files/increment` に配布された 3 CSV がそろっていることを確認します。待機用フォルダーにしかない場合は、先に第 12〜13 章の取り込みを完了します。Eventhouse に行があるだけでは、Notebook 05 の CSV 入力要件を満たしません。": "Verify that all 3 distributed CSVs are present in `Files/increment` in the target Lakehouse. If they exist only in the staging folder, first complete ingestion in Chapters 12–13. Rows in Eventhouse alone do not satisfy Notebook 05's CSV input requirements.",
 "PARTICIPANT_ID・EXPECTED_WORKSPACE_NAME を設定し、STRICT_SYNTHETIC_CONTRACT=True とAPPLY_CHANGES=False で preview を実行します。PLAN_SHA256 を確認してから、APPLY_CHANGES=True・CONFIRMED_PLAN_SHA256・EXCLUSIVE_APPLY_WINDOW_CONFIRMED=True で対話実行します。": "Set PARTICIPANT_ID and EXPECTED_WORKSPACE_NAME, then run preview with STRICT_SYNTHETIC_CONTRACT=True and APPLY_CHANGES=False. After verifying PLAN_SHA256, run interactively with APPLY_CHANGES=True, CONFIRMED_PLAN_SHA256, and EXCLUSIVE_APPLY_WINDOW_CONFIRMED=True.",
 "ANALYTICS_READY と `ops.analytics_publish_control` の Ready を確認します。GENERATION_ID、公開テーブル、DQ 結果、quarantine 件数を記録し、Preparing / Publishing のままなら Power BI や Agent の入力にしません。": "Verify ANALYTICS_READY and Ready in `ops.analytics_publish_control`. Record GENERATION_ID, published tables, DQ results, and quarantine counts. If the state remains Preparing / Publishing, do not use the outputs as input to Power BI or an Agent.",
 "D.3.3 Power BI は別工程で配置する": "D.3.3 Deploy Power BI in a separate step",
 "先に Notebook 05 の Ready を確認し、SQL analytics endpoint で必要な gold テーブルが参照できることも確認します。新しい schema が未反映ならメタデータの反映・更新を待ってから進みます。これらは手順上の前提であり、配置スクリプトが `ops.analytics_publish_control` を自動検査するわけではありません。": "First verify Notebook 05's Ready state and confirm that the required gold tables are accessible through the SQL analytics endpoint. If new schemas are not yet reflected, wait for metadata synchronization or refresh before proceeding. These are procedural prerequisites; the deployment script does not automatically inspect `ops.analytics_publish_control`.",
 "Notebook 05 の分析用カレンダーは、対象となる各暦年の年初から年末まで日付が連続することを確認します。最後の寄付日・観測日で打ち切らず、日付の重複・空値・欠落も認めません。修正はカレンダー生成側で行い、DAX の期間補正で隠しません。カレンダー行が存在しても、その日付に寄付・観測があることは意味しません。付録 E の Power BI date-table design guidance も確認してください。": "Verify that Notebook 05's analytics calendar contains continuous dates from the start to the end of every calendar year in scope. Do not stop at the final donation or observation date, and do not allow duplicate, blank, or missing dates. Correct calendar generation rather than hiding the issue with a DAX period adjustment. A calendar row does not imply that donations or observations exist on that date. Also consult Power BI date-table design guidance in Appendix E.",
 "カレンダーの年初・年末とデータの観測範囲を区別する": "Distinguish full calendar years from the data observation range",
 "Notebook 05 が作るのは分析テーブルであり、SemanticModel / Report item ではありません。Ready の確認後、ファシリテーターが `tools/powerbi/Deploy-FurusatoPowerBI.ps1` を WorkspaceId・ParticipantId・実際の LakehouseId・FolderId で preview し、対象と定義を確認してから Apply と確認フレーズ `DEPLOY POWER BI <PID>` を指定します。ExpectedUserPrincipalName と ExpectedTenantId は実行環境の値で指定し、認証主体の取り違えを防ぎます。これらの実値は配布物へ書き込みません。配布 PBIP は移植用の byPath 構成なので、Power BI Desktop の Publish で代用しません。": "Notebook 05 creates analytics tables, not SemanticModel / Report items. After verifying Ready, the facilitator previews `tools/powerbi/Deploy-FurusatoPowerBI.ps1` with WorkspaceId, ParticipantId, and the actual LakehouseId and FolderId, checks the targets and definitions, then supplies Apply and the confirmation phrase `DEPLOY POWER BI <PID>`. Set ExpectedUserPrincipalName and ExpectedTenantId to the execution environment's values to avoid using the wrong identity. Do not write those actual values into the distribution. The distributed PBIP uses a portable byPath configuration; do not substitute Power BI Desktop Publish.",
 "FolderId は対象 Lakehouse と同じ Folder の実 GUID を指定します。省略時は Lakehouse の配置先から取得しますが、実 GUID がない場合や別 Folder を指定した場合は停止します。Lakehouse が Workspace 直下なら、参加者の対象 Lakehouse を許可された Folder に移し、実 ID と接続を再確認します。新規モデルとレポートはその Folder に作成されます。既存 item の更新は、対象と影響を確認した場合だけ `-UpdateExisting` を明示します。この指定は他の Folder の同名 item の更新を許可するものではありません。": "Set FolderId to the actual GUID of the target Lakehouse's Folder. If omitted, it is obtained from the Lakehouse's location; missing a real GUID or specifying another Folder stops execution. If the Lakehouse is at the Workspace root, move the participant's target Lakehouse into an authorized Folder and recheck its actual ID and connections. New models and reports are created in that Folder. Explicitly supply `-UpdateExisting` only for an existing-item update whose target and impact you have reviewed. This option does not authorize updates to same-named items in other Folders.",
 "Power BI の Folder と更新対象を preview で確認する": "Verify the Power BI Folder and update targets in preview",
 "配置後に SemanticModel / Report の実 ID と最終 Folder、Report が参照する SemanticModel ID、Direct Lake が参照する Workspace / Lakehouse ID を確認します。": "After deployment, verify the actual SemanticModel / Report IDs and final Folder, the SemanticModel ID referenced by the Report, and the Workspace / Lakehouse IDs referenced by Direct Lake.",
 "Fabric 上で Direct Lake の接続・DAX の結果・レポート表示を確認します。REST の作成成功や Report の存在だけでは、データを読めた証拠にはなりません。前年同期間は `SAMEPERIODLASTYEAR` の値を、前年の開始日・終了日を明示した SQL 集計と照合します。月初や月の途中で終わる範囲も試し、意図せず月末まで拡張されていないか確認します。": "Verify Direct Lake connectivity, DAX results, and report rendering in Fabric. Successful REST creation or the existence of a Report alone is not evidence that data was read. Reconcile the `SAMEPERIODLASTYEAR` value against SQL aggregation with explicit start and end dates in the previous year. Also test ranges ending at the start or middle of a month, checking that they do not unexpectedly extend to month-end.",
 "Core Data Agent の選択テーブルは増やしません。`gold.donation_agent` を試す場合は別の評価構成として記録し、Core の 3 ソースと held-out の前提を保ちます。": "Do not expand the Core Data Agent's selected tables. If testing `gold.donation_agent`, record it as a separate evaluation configuration while preserving Core's 3 sources and held-out assumptions.",
 "D.3.4 Gold を Data Agent で評価する場合": "D.3.4 Evaluating Gold with a Data Agent",
 "`gold.donation_agent` の実際の区分列は `DataSource` で、`StaticSeed` と `RealtimeIncrement` が同居します。`SourceDataset` は SQL 出力の別名として使われることがあっても、このテーブルの実列名ではありません。Gold を選ぶ評価用 Agent に、Lakehouse は静的 2025 スナップショットだけという説明を流用しません。区分・期間・粒度を明示し、片方を求められたら DataSource で絞ります。RealtimeIncrement は重複排除・整形済みの分析データで、Core Eventhouse の raw 観測と同じ指標ではありません。": "The actual partition-label column in `gold.donation_agent` is `DataSource`, with both `StaticSeed` and `RealtimeIncrement` present. Although `SourceDataset` may be used as a SQL output alias, it is not a physical column in this table. Do not reuse a description saying the Lakehouse contains only the static 2025 snapshot for an evaluation Agent that selects Gold. State the partition, period, and grain, and filter by DataSource when only one partition is requested. RealtimeIncrement is de-duplicated, curated analytical data, not the same metric as Core Eventhouse raw observations.",
 "Gold の混在データをすべて静的と説明しない": "Do not describe all mixed Gold data as static",
 "Core の指示・選択テーブル・few-shot は変更せず、別の評価用 Data Agent で同じ質問による前後比較を行います。単発の前後比較だけで改善や因果関係を結論づけません。空の結果とソース障害は別です。許可なくソースを削除・停止したり権限を変えたりせず、実施していない障害試験は未実施と記録します。": "Leave Core instructions, selected tables, and few-shots unchanged, and compare the same questions before and after changes in a separate evaluation Data Agent. Do not conclude improvement or causality from a single before-and-after comparison. Empty results and source failures are different. Do not delete or stop sources or change permissions without authorization; record outage tests that were not performed as not run.",
 "User data functions がメニューに表示されない場合は、Agent への接続・Agent 経由の呼び出し・接続後の 10 問をブロックとして記録します。関数単体の公開・呼び出しの成功とは区別し、利用できる Code Interpreter の実習まで同じ判定にまとめません。表示されない理由を推測してテナント設定や権限を変更しません。": "If User data functions is absent from the menu, record Agent binding, Agent-mediated invocation, and the 10 questions after binding as blocked. Distinguish this from successful standalone function publication and invocation; do not apply the same verdict to an available Code Interpreter exercise. Do not change tenant settings or permissions based on a guessed reason for the missing option.",
 "利用不可の UDF 接続を単体成功で置き換えない": "Do not substitute standalone success for unavailable UDF binding",
 "実施する場合は、対象 Workspace / Folder に User data functions item を作成して配布 Python を取り込み、関数単体のテストと公開を先に行います。0・2,000 円・それ以上の入力と負値の扱いを確認し、実行結果を記録してから Data Agent の［Add tools］で公開済みの関数を接続します。接続後は Agent から対象関数が呼ばれたことも確認し、関数単体の成功で代用しません。メニューや接続先が利用できない場合はブロックと記録し、Python ファイルを置いただけで接続済みとはしません。": "To perform the exercise, create a User data functions item in the target Workspace / Folder, import the distributed Python, and first test and publish the function itself. Check inputs of 0, 2,000 JPY, and higher amounts, as well as handling of negative values. Record the results before connecting the published function through the Data Agent's ［Add tools］. After connection, also verify that the Agent invoked the target function; standalone function success is not a substitute. If the menu or connection target is unavailable, record the step as blocked; placing a Python file alone does not mean the function is connected.",
 "テンプレートの取り込みだけでは Notebook のパラメーターや Pipeline に値は自動接続されません。アクティブな値セットと NotebookUtils で参照した実際の値を確認し、どの実行で使ったかを記録します。配置、値セットの有効化、実行時の参照確認を分けて扱ってください。": "Importing the template alone does not automatically bind values to Notebook parameters or a Pipeline. Verify the active value set and the actual values resolved through NotebookUtils, and record which execution used them. Treat deployment, value-set activation, and runtime reference verification as separate steps.",
 "Variable Library の配置と利用を分ける": "Distinguish Variable Library deployment from use"
}
